img_dir:      dataset/card/images
ann_dir:      dataset/card/annotations

num_classes:  2
class_map:    {cccd: 1}

epochs:       50
batch_size:   2
lr:           0.005
max_size:     800
score_thresh: 0.4

weights_dir:  weights/card